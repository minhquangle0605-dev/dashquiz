/**
 * Planned Zod schemas (academic module):
 * - subjectBodySchema — name, code, description
 * - subjectIdParamSchema
 * - academicYearBodySchema — label, startDate, endDate, isCurrent
 * - academicYearIdParamSchema
 * - semesterBodySchema — name, startDate, endDate, orderIndex
 * - listSubjectsQuerySchema — search, page, limit
 * - listAcademicYearsQuerySchema — includeArchived
 */

export const academicSchemas = {} as const;
