/**
 * Planned Zod schemas (analytics module):
 * - studentDashboardQuerySchema — dateRange, subjectId
 * - teacherDashboardQuerySchema — classId, dateRange, examId
 * - strengthsQuerySchema — scope, id, subjectId
 * - timeOnTaskParamSchema — attemptId
 * - exportReportBodySchema — reportType, format, filters
 */

export const analyticsSchemas = {} as const;
