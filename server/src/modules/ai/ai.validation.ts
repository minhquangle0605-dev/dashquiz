/**
 * Planned Zod schemas (AI module):
 * - startPracticeBodySchema — topics[], maxItems, difficulty
 * - sessionIdParamSchema
 * - recordOutcomeBodySchema — questionId, selectedOptionId, timeSpentMs
 * - wrongAnswerContextQuerySchema — attemptId, limit
 */

export const aiSchemas = {} as const;
