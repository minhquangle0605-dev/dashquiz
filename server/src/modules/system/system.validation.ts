/**
 * Planned Zod schemas (system module):
 * - updateConfigBodySchema — key, value (json), reason
 * - listActivityLogsQuerySchema — actorId, action, from, to, page, limit
 * - backupBodySchema — destination, includeUploads
 * - restoreBodySchema — backupId, confirmToken
 * - configKeyParamSchema
 */

export const systemSchemas = {} as const;
