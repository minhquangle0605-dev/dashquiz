/**
 * Planned Zod schemas (class module):
 * - createClassBodySchema — name, code, academicYearId, subjectIds[]
 * - updateClassBodySchema — partial fields
 * - classIdParamSchema
 * - enrollBodySchema — studentId | studentIds[]
 * - unenrollBodySchema — studentId
 * - listClassesQuerySchema — academicYearId, teacherId, page, limit
 */

export const classSchemas = {} as const;
